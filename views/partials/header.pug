header.header
	.header-column
		h1
			a(href=routes.home)
				i.fab.fa-youtube
				| #{siteName}
	.header-column
		form(action=routes.search method="get")
			input(type="text" name="term")
	.header-column
		ul
			li
				a(href="#")
					| Login
			li
				a(href=routes.join)
					| Join